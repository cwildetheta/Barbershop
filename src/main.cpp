#include "manager.h"

int main()
{
    system("cls");
    srand(clock());
    manager barbershop(4, 12, 3, 30, 3);
    /*for(int i = 0; i < 256; i++){
        std::cout << i << ": " << char(i) << "  ";
    }*/
    return 0;
}